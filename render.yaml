previews:
  generation: automatic

services:
  - type: web
    name: web
    plan: free
    envVars:
      - key: NODE_ENV
        value: production

  - type: web
    name: playground
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
